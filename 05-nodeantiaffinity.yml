apiVersion: v1
kind: Pod
metadata:
  name: nginx-node-anti-affinity
  labels:
    env: test
spec:
  affinity:
    nodeAffinity:
      #hard rule
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
        - matchExpressions:
          - key: topology.kubernetes.io/zone
            operator: NotIn
            values:
            - us-east-1b
          - key: project
            operator: In
            values:
            - roboshop     
  containers:
  - name: nginx
    image: nginx
  # if a node is tainted u have to give this pod to permission to enter into that node   
  tolerations:
  - key: "project"
    operator: "Equal"
    value: "roboshop"
    effect: "NoExecute"